mixin rate-btn(option)
  input.rating-buttons__input(
    type='radio'
    name= option.name || 'star'
    id= option.id
    checked= option.checked || false
  )
  label.rating-buttons__label(for= option.id)